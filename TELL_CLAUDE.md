# Paste this into Claude Code / Claude in VS Code

I attached `keanu-site.zip`. This is a finished website. Do not redesign it.

Do this:

1. Unzip `keanu-site.zip` into the current workspace root. Overwrite if files already exist.
2. Run `npm install`.
3. Run `npm run dev`.
4. Confirm the site opens. Hero copy must read:
   - AI specialist
   - Workflow automation
   - Automation engineering for operators who need the system to run.
5. If install or dev fails, fix only the error. Do not change copy, colors, rooms, or motion.

Do not add sections. Do not put my name on the doorway. Do not replace the warp with normal scrolling.

If you need the spec, read `KEANU_MASTER_PROMPT.md`.
